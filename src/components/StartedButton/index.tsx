/* eslint-disable react/no-unescaped-entities */

import { Button } from './stated-button.styles';

function StartedButton() {
  return (
    <Button to="/started">
      Get Started <span>- it's free</span>
    </Button>
  );
}

export default StartedButton;
